# Copy this file to database.env (gitignored) and paste your Railway Postgres URL.
# Railway → Postgres plugin → Variables → DATABASE_URL (or Connect → Database URL).
#
# One connection string, no quotes:
# postgresql://USER:PASSWORD@HOST:PORT/railway?sslmode=require
